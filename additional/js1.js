#1.Create a new Mongo Database, load 6 "documents" into a Mongo Collection all at once, show how to delete the 3rd, and 5th documents, only leaving 4 left.
#2. What is Hoisting in Javascript? 
    js automatically brings variable definition to top of file prior to interpreting the rest of the file
#3. What are the differences between var, let, and const? 
        var  : can be redefined or re-assigned
        let  : allows for local definition of variables can be re-assigned but not re-defined
        const: immutable variable, cannot be re assigned or re-defined in a standard sense
#4. What are some datatypes used in JavaScript? 
        string,integer,float,array
#5. Create a variable called z, assign x + y to it, and display the result in the console. 
      let z = x + Y 
        console.log(z)
#6. Display the sum of 5 + 10, using two variables: x and y. Print the result to the console.
    let sum = 5+10
    console.log(sum)
    
